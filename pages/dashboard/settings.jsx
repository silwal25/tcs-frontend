import Topbar from "../../components/TopBar/Topbar"

const Settings = () => {
  return (
    <div>
      <Topbar heading="Settings" />
    </div>
  )
}

Settings.layout = "dash"

export default Settings
